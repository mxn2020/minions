{
  "name": "minions",
  "private": true,
  "version": "0.1.0",
  "description": "A universal structured object system for AI agents",
  "license": "AGPL-3.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/cli && npm run build -w apps/docs",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run build -w packages/core && npm run lint -w packages/core && npm run lint -w packages/cli"
  },
  "engines": {
    "node": ">=18"
  }
}
