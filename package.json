{
  "name": "minions",
  "private": true,
  "version": "0.1.0",
  "description": "A universal structured object system for AI agents & systems",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm --filter @minions/core run build && pnpm --filter @minions/cli run build && pnpm --filter @minions/docs run build && pnpm --filter @minions/web run build",
    "test": "pnpm -r run test",
    "lint": "pnpm --filter @minions/core run build && pnpm --filter @minions/core run lint && pnpm --filter @minions/cli run lint"
  },
  "engines": {
    "node": ">=18"
  }
}